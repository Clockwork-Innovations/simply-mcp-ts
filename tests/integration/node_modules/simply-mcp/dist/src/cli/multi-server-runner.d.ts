/**
 * Multi-server runner for SimplyMCP
 * Manages multiple MCP servers running simultaneously
 */
import { type ChildProcess } from 'node:child_process';
/**
 * Options for running a single server in multi-server mode
 */
export interface ServerRunOptions {
    filePath: string;
    port?: number;
    useHttp: boolean;
    useHttpStateless?: boolean;
    verbose: boolean;
    style?: string;
    color?: string;
    groupId: string;
}
/**
 * Running server instance
 */
export interface RunningServer {
    filePath: string;
    process: ChildProcess;
    port?: number;
    name: string;
    color: string;
    groupId: string;
}
/**
 * Options for multi-server run
 */
export interface MultiServerOptions {
    files: string[];
    useHttp: boolean;
    useHttpStateless?: boolean;
    startPort?: number;
    verbose: boolean;
    forceStyle?: string;
}
/**
 * Run multiple servers simultaneously
 */
export declare function runMultipleServers(options: MultiServerOptions): Promise<void>;
//# sourceMappingURL=multi-server-runner.d.ts.map