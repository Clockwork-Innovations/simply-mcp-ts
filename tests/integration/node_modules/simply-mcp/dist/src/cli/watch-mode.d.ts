/**
 * Watch mode manager for SimplyMCP CLI
 * Watches for file changes and auto-restarts the server
 */
import type { APIStyle } from './run.js';
/**
 * Options for watch mode
 */
export interface WatchModeOptions {
    file: string;
    style: APIStyle;
    http: boolean;
    httpStateless?: boolean;
    port: number;
    poll: boolean;
    interval: number;
    verbose: boolean;
}
/**
 * Start watch mode
 */
export declare function startWatchMode(options: WatchModeOptions): Promise<void>;
//# sourceMappingURL=watch-mode.d.ts.map