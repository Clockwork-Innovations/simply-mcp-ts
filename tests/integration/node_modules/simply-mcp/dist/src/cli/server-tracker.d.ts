/**
 * Server tracker for managing multiple running MCP servers
 * Tracks server processes, ports, and metadata
 */
/**
 * Server information tracked in the registry
 */
export interface ServerInfo {
    /** Server name from config */
    name: string;
    /** Process ID */
    pid: number;
    /** Path to server file */
    filePath: string;
    /** Transport type */
    transport: 'stdio' | 'http';
    /** HTTP port (if using HTTP transport) */
    port?: number;
    /** Server version */
    version?: string;
    /** Start time */
    startedAt: number;
    /** Whether this is part of a multi-server run */
    isMulti?: boolean;
    /** Group ID for multi-server runs */
    groupId?: string;
}
/**
 * Server registry stored in temp directory
 */
export interface ServerRegistry {
    servers: ServerInfo[];
    lastUpdated: number;
}
/**
 * Read the server registry
 */
export declare function readRegistry(): Promise<ServerRegistry>;
/**
 * Write the server registry
 */
export declare function writeRegistry(registry: ServerRegistry): Promise<void>;
/**
 * Register a new server
 */
export declare function registerServer(info: ServerInfo): Promise<void>;
/**
 * Unregister a server by PID
 */
export declare function unregisterServer(pid: number): Promise<void>;
/**
 * Unregister all servers in a group
 */
export declare function unregisterGroup(groupId: string): Promise<void>;
/**
 * Get all registered servers
 */
export declare function listServers(): Promise<ServerInfo[]>;
/**
 * Get servers in a specific group
 */
export declare function getGroupServers(groupId: string): Promise<ServerInfo[]>;
/**
 * Check if a process is still running
 */
export declare function isProcessAlive(pid: number): boolean;
/**
 * Clean up dead servers from the registry
 */
export declare function cleanupDeadServers(): Promise<number>;
/**
 * Get a server by PID
 */
export declare function getServerByPid(pid: number): Promise<ServerInfo | undefined>;
/**
 * Get servers by name pattern
 */
export declare function getServersByName(namePattern: string): Promise<ServerInfo[]>;
/**
 * Check if a port is in use
 */
export declare function isPortInUse(port: number): Promise<boolean>;
/**
 * Find next available port starting from a given port
 */
export declare function findAvailablePort(startPort: number): Promise<number>;
/**
 * Generate a unique group ID for multi-server runs
 */
export declare function generateGroupId(): string;
//# sourceMappingURL=server-tracker.d.ts.map