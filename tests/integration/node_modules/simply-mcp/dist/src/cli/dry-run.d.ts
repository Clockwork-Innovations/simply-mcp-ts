/**
 * Dry-run mode for validating server configuration without starting
 * Validates server files, detects API style, and extracts metadata
 */
import type { APIStyle } from './run.js';
/**
 * Result of dry-run validation
 */
export interface DryRunResult {
    success: boolean;
    detectedStyle: APIStyle;
    serverConfig: {
        name: string;
        version: string;
        port?: number;
    };
    tools: Array<{
        name: string;
        description?: string;
    }>;
    prompts: Array<{
        name: string;
        description?: string;
    }>;
    resources: Array<{
        name: string;
        description?: string;
    }>;
    transport: 'stdio' | 'http';
    portConfig: number;
    warnings: string[];
    errors: string[];
}
/**
 * Main entry point for dry-run mode
 */
export declare function runDryRun(filePath: string, style: APIStyle, useHttp: boolean, port: number, jsonOutput?: boolean): Promise<void>;
export declare function dryRunDecoratorAdapter(filePath: string, useHttp: boolean, port: number): Promise<void>;
export declare function dryRunFunctionalAdapter(filePath: string, useHttp: boolean, port: number): Promise<void>;
export declare function dryRunProgrammaticAdapter(filePath: string, useHttp: boolean, port: number): Promise<void>;
//# sourceMappingURL=dry-run.d.ts.map