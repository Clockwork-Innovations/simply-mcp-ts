/**
 * Dependency installer - Execute package manager to install dependencies
 * Feature 3: Auto-Installation
 */
import { InstallOptions, InstallResult } from '../features/dependencies/installation-types';
/**
 * Install dependencies using detected package manager
 *
 * @param dependencies - Dependencies to install (package -> version map)
 * @param options - Installation options
 * @returns Installation result
 *
 * @example
 * ```typescript
 * const result = await installDependencies({
 *   'axios': '^1.6.0',
 *   'zod': '^3.22.0'
 * });
 * console.log(result.installed); // ['axios@^1.6.0', 'zod@^3.22.0']
 * ```
 */
export declare function installDependencies(dependencies: Record<string, string>, options?: InstallOptions): Promise<InstallResult>;
//# sourceMappingURL=dependency-installer.d.ts.map