export interface StandaloneOptions {
    bundlePath: string;
    outputDir: string;
    includeAssets?: string[];
    includeNativeModules?: boolean;
    dependencies?: Record<string, string>;
}
/**
 * Create standalone bundle (directory format)
 */
export declare function createStandaloneBundle(options: StandaloneOptions): Promise<{
    outputDir: string;
    files: string[];
}>;
//# sourceMappingURL=standalone-formatter.d.ts.map