/**
 * Error Message Templates
 *
 * Provides consistent, actionable error messages across the codebase.
 * All error messages follow the pattern:
 * 1. Clear description of what went wrong
 * 2. Expected vs. actual (where applicable)
 * 3. Example of correct usage
 * 4. Troubleshooting steps
 * 5. Link to documentation
 */
/**
 * Standard error message templates for common scenarios
 */
export declare const ErrorMessages: {
    /**
     * Error when no valid MCP server class is found in a file
     */
    INVALID_SERVER_CLASS: (file: string) => string;
    /**
     * Error when file cannot be loaded
     */
    FILE_LOAD_ERROR: (file: string, error: string, resolvedPath?: string) => string;
    /**
     * Error when class is not decorated with @MCPServer
     */
    MISSING_SERVER_DECORATOR: (className: string) => string;
    /**
     * Error when tool configuration is invalid
     */
    INVALID_TOOL_CONFIG: (toolName: string, issue: string) => string;
    /**
     * Error when tool is already registered
     */
    DUPLICATE_TOOL: (toolName: string) => string;
    /**
     * Error when prompt configuration is invalid
     */
    INVALID_PROMPT_CONFIG: (promptName: string, issue: string) => string;
    /**
     * Error when duplicate prompt is registered
     */
    DUPLICATE_PROMPT: (promptName: string) => string;
    /**
     * Error when resource configuration is invalid
     */
    INVALID_RESOURCE_CONFIG: (resourceUri: string, issue: string) => string;
    /**
     * Error when duplicate resource is registered
     */
    DUPLICATE_RESOURCE: (resourceUri: string) => string;
    /**
     * Error when server is already running
     */
    SERVER_ALREADY_RUNNING: () => string;
    /**
     * Error when server fails to initialize
     */
    SERVER_INIT_ERROR: (error: string) => string;
    /**
     * Error when transport fails to start
     */
    TRANSPORT_ERROR: (transport: string, error: string) => string;
    /**
     * Error when adding items after server has started
     */
    CANNOT_ADD_AFTER_START: (itemType: "tool" | "prompt" | "resource") => string;
};
/**
 * Format an error with additional context
 *
 * @param message - The error message
 * @param context - Additional context to include
 * @returns Formatted error message with context
 */
export declare function formatError(message: string, context?: Record<string, any>): string;
/**
 * Create a helpful error for missing configuration
 *
 * @param field - The missing field name
 * @param location - Where the field should be (e.g., "server options", "tool definition")
 * @returns Error message
 */
export declare function missingFieldError(field: string, location: string): string;
/**
 * Create a helpful error for type mismatches
 *
 * @param field - The field name
 * @param expected - Expected type
 * @param received - Actual type received
 * @returns Error message
 */
export declare function typeMismatchError(field: string, expected: string, received: string): string;
//# sourceMappingURL=error-messages.d.ts.map