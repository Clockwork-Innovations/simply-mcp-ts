/**
 * Main bundler implementation using esbuild
 * Feature 4: Bundling Command
 */
import * as esbuild from 'esbuild';
import { BundleOptions, BundleResult } from '../features/dependencies/bundle-types';
/**
 * Bundle a SimplyMCP server using esbuild
 *
 * @param options - Bundle options
 * @returns Bundle result
 *
 * @example
 * ```typescript
 * const result = await bundle({
 *   entry: './server.ts',
 *   output: './dist/server.js',
 *   format: 'single-file',
 *   minify: true
 * });
 *
 * if (result.success) {
 *   console.log(`Bundled to ${result.outputPath} (${result.size} bytes)`);
 * }
 * ```
 */
export declare function bundle(options: BundleOptions): Promise<BundleResult>;
/**
 * Stop watching (for watch mode)
 * Note: This would need to be exposed via a context object
 */
export declare function stopWatch(context: esbuild.BuildContext): Promise<void>;
//# sourceMappingURL=bundler.d.ts.map