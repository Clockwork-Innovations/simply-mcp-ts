/**
 * Output formatting for different bundle formats
 * Handles standalone, executable, and other output formats
 */
import { BundleOptions, ResolvedDependencies } from '../features/dependencies/bundle-types';
import * as esbuild from 'esbuild';
/**
 * Format output based on bundle format
 *
 * @param options - Bundle options
 * @param buildResult - esbuild build result
 * @param deps - Resolved dependencies
 */
export declare function formatOutput(options: BundleOptions, buildResult: esbuild.BuildResult, deps: ResolvedDependencies): Promise<void>;
/**
 * Create a shell script wrapper for easier execution
 */
export declare function createShellWrapper(bundlePath: string, name?: string): Promise<string>;
/**
 * Create a Windows batch wrapper
 */
export declare function createBatchWrapper(bundlePath: string, name?: string): Promise<string>;
//# sourceMappingURL=output-formatter.d.ts.map