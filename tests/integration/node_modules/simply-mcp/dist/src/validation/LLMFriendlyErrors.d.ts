/**
 * LLMFriendlyErrors.ts
 * Enhanced error messages designed for LLM self-healing
 *
 * Provides:
 * - Clear schema expectations
 * - What was received vs expected
 * - Actionable fix instructions
 * - Valid examples
 */
import { JsonSchemaDefinition } from './JsonSchemaToZod.js';
import { ValidationErrorDetail } from './ValidationError.js';
/**
 * LLM-friendly error response format
 */
export interface LLMFriendlyErrorResponse {
    error: {
        type: 'validation' | 'handler' | 'system';
        summary: string;
        issues: Array<{
            field: string;
            problem: string;
            received: any;
            expected: string;
            fix: string;
        }>;
        schema: {
            description: string;
            properties: Record<string, PropertyHelp>;
            required: string[];
            examples?: any[];
        };
        suggestions: string[];
        validExample: any;
    };
}
interface PropertyHelp {
    type: string;
    description: string;
    constraints: string[];
    examples: any[];
}
/**
 * Generate LLM-friendly validation error
 */
export declare function createLLMFriendlyValidationError(toolName: string, toolDescription: string, schema: JsonSchemaDefinition, errors: ValidationErrorDetail[], receivedInput: any): LLMFriendlyErrorResponse;
/**
 * Format error for LLM consumption
 */
export declare function formatErrorForLLM(response: LLMFriendlyErrorResponse): string;
export {};
//# sourceMappingURL=LLMFriendlyErrors.d.ts.map